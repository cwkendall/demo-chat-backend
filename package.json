{
  "name": "chat-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Twilio Conversations sample backend service",
  "keywords": ["Twilio", "Conversations"],
  "repository": "https://github.com/cwkendall/demo-chat-backend",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "twilio": "^3.34.0"
  }
}
